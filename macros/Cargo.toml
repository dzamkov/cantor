[package]
name = "cantor_macros"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "cantor_macros"
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "1.0", features = ["parsing"] }